[project]
name = "political-fundraising-emails"
version = "0.2.0"
description = "Extract and compare political committee names from fundraising emails using local LLMs"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "llm>=0.26",
    "rapidfuzz>=3.12",
]
